[pytest]
# Only look for tests in the project `tests/` directory and skip the large
# knowledge base/third-party content which pulls in heavy optional packages.
testpaths = tests
norecursedirs = knowledge_base

# Keep default python test file discovery but you can adjust if needed
# python_files = test_*.py
